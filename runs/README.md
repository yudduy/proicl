# Runs Directory

Generated experiment artifacts live here locally but are not tracked by Git.

Tracked exception:

- `progress.md`: dated decisions, observed evidence, and operational state required by the protocol.

Large artifacts, calibration bundles, trajectory caches, SQLite memory stores, logs, and remote mirrors should stay outside Git. Archive them with external object storage or release artifacts when needed.
